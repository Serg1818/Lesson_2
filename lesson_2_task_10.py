X = float() # размер вклада
Y = int() # количество лет
p = int() # процентная ставка
def bank(X, Y, p):
    X = X * (pow((1 + p / 100), Y))
    print ('Размер вклада за', Y, 'лет составит', round(X, 2), 'рублей')
bank(10000, 6, 10)
